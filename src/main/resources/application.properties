spring.application.name=ddvs

# DB
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres.xsanffkcnpcilvuvfrif}
spring.datasource.password=${DATABASE_PASSWORD:Plutoski@88}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.defer-datasource-initialization=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server
server.port=10000

# JWT
jwt.secret=${JWT_SECRET:ddvs-super-secret-jwt-key-2026-production}
jwt.expiration=86400000

# Notification
notification.service.url=https://notification-service-4p41.onrender.com